services:
  immich-auto-tag:
    image: ghcr.io/kinetic-it/immich-auto-tag:latest
    # Or build locally instead:
    # build: .
    container_name: immich-auto-tag
    restart: "no"
    env_file:
      - .env
